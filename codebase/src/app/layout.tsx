/**
 * Stub layout component — Next.js App Router style placeholder.
 * Currently a pass-through; kept for folder-structure compatibility
 * with the HV4 spec. Real layout logic will be added in later steps.
 */
export default function Layout() {
  return null;
}
